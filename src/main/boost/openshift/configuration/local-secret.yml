secrets:
  - name: JBOSS_A_MQ_BROKER_PASSWORD
    value: amq
  - name: JBOSS_A_MQ_BROKER_USER
    value: amq
  - name: CONSOLE_FEATURE_API_DB_PASSWORD
    value: welcome1
  - name: CONSOLE_FEATURE_API_DB_USER
    value: postgres
  - name: OPENSHIFT_USER
    value: "{{ openshift.username }}"
  - name: OPENSHIFT_PASSWORD
    value: "{{ openshift.password }}"    
  - name: GITHUB_USER
    value: "{{ github.user }}"
  - name: GITHUB_PASSWORD
    value: "{{ github.password }}"     